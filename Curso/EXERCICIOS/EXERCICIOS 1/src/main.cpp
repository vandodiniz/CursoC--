#include <iostream>
#define pi 3.1415

using namespace std;

int main() {
    
/*EX 1 e 2:
    float raio, per, ar, v;
    cout << "\nDigite o raio = ";
    cin >> raio;
    per = 2 * pi * raio;
    ar = pi * raio * raio;

    cout << "perimetro = " << per << "\narea = " << ar;

    v = (4*pi*raio*raio*raio)/3;
    cout << "\nvolume = " << v;
*/

/*EX 3:
    int anos, dias;
    cin >> anos;
    dias = anos * 365;

    cout << "tempo total = " << dias << " dias";
*/

/*EX 4:
    int horas, minutos;

    cin >> horas;
    cin >> minutos;

    cout << "\nminutos totais = " << horas * 60 + minutos;
    cout << "\nsegundos totais = " << (horas * 60 + minutos)*60;
*/

/*EX 5 e 6:
    float C, F;

    cout << "\nDigite a temp em celsius = ";
    cin >> C;

    F = 9*C/5 +32;

    cout << "\nTemp em F = " << F;


    cout << "\nDigite a temp em F = ";
    cin >> F;

    C = (F-32)*5/9;

    cout << "\nTemp em C = " << C;
*/



}